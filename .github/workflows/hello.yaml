name: First Workflow

on:
  push:

jobs:
  greet_stage_1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello from Github Actions Runner"
